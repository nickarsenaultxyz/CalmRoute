# Core data analysis
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.5,<2

# Geospatial
geopandas>=0.14.0
pyogrio>=0.7.0
shapely>=2.0.0

# Network analysis
networkx>=3.0

# Testing
pytest>=8.0.0

# Params are read with stdlib tomllib (Python 3.11+); no TOML dependency needed.
#
# Removed deliberately — none of these were ever imported by the pipeline:
#   osmnx, rasterio, rio-vrt   leftovers from an abandoned OSM/elevation approach
#   matplotlib, requests, python-dateutil
#   folium                     the map is now a hand-written MapLibre page
